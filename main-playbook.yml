    - name: Barebones-Playbook
      hosts: debian-server
      remote_user: "{{ user_name }}"
      gather_facts: yes
      vars_files: ./group_vars/variables.yml

      roles:
        - packages
        - ssh
        - unattended-upgrades
        - mail
        - lynis
        - nftables
        - fail2ban

